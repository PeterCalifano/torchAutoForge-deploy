# torchAutoForge-deploy C++ API

This documentation covers the backend-neutral inference facade, ONNX Runtime
backend, optional TensorRT engine backend, model-role adapters, wrapper-safe
values, CUDA/PTX examples, and project logging API.

## Installation

## Example usage (assuming installation worked)
